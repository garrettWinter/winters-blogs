<div id="blogPostSection">
    {{!-- Pulls in header details about the post --}}
    {{#each postDetails as |post|}}
    <article class="blogPostCard">
        <div class="commentHeader" data-postID="{{post.post_id}}">
            <div class="postTitle">{{post.User.user_name}} Posted "{{post.post_title}}" on
                {{post.createdAt}}</div>
        </div>
        <div class="postBody"> {{post.post_body}}</div>
    </article>
    {{/each}}
    {{!-- Populates the comments that are associated to this post --}}
    {{#each commentDetails as |posts|}}
    <article class="blogPostCard">
        <div class="commentHeader">
            <div class="postTitle">{{posts.User.user_name}} commented
                {{posts.updatedAt}}</div>
        </div>
        <div class="postBody"> {{posts.comment}}</div>
    </article>
    {{/each}}
</div>
{{#if loggedIn }}
<div id="newCommentBox">
    <textarea id="addCommentTextBox"></textarea>
    <button id="newCommentBtn" data="{{commentDetails.Post.post_id}}">Add a Comment!</button>
</div>
<script src="/js/comment.js"></script>
{{/if}}
{{#unless loggedIn}}
<div id="newPostBox">
    <button id="loginBtn"><a href="/login">Login to add a comment</a></button>
</div>
{{/unless}}